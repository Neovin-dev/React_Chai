function Chai(){
    return(
        <h3>Chai is ready</h3>
        // when we have to return more than one element we wrap that inside div but in recent react we wrap them in framgments like <>
    )
}

export default Chai